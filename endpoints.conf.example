# endpoints.conf.example
# INI-like file. Sections are endpoint names in [brackets].
# Keys:
# endpoint = base URL
# api_key = API key/token (prefer to leave empty and use env vars instead)
# list_models_cmd = optional command to list models (run locally)

[copilot-api]
endpoint=https://10.189.8.10:4142
api_key=
api_key_env=API_KEY_COPILOT
list_models_cmd=
keep_proxy_config=false
